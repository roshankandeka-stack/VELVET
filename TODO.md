- [ ] Identify the build/runtime error causing the “problem”.
- [ ] Update code to fix the root cause.
- [ ] Ensure TypeScript compiles (npm run lint / tsc --noEmit).
- [ ] Run the dev server and verify the fixed behavior.

